# Architecture Notes

This prototype starts with a single backend service focused on project-scoped upload/retrieval and role-aware workflow control.

Future iterations should split storage adapters, policy engine, and review services into separate modules.
